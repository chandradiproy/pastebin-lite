{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/client/dist/index.html"
    }
  ],
  "buildCommand": "cd client && npm install && npm run build",
  "devCommand": "cd api && npm start && cd ../client && npm run dev",
  "outputDirectory": "client/dist"
}
