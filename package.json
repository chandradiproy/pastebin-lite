{
  "name": "pastebin-lite-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "nanoid": "^5.0.7",
    "pg": "^8.11.5"
  },
  "engines": {
    "node": "18.x"
  }
}
